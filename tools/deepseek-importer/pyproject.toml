[project]
name = "athena-deepseek-importer"
version = "0.1.0"
description = "DeepSeek importer for Athena Tool Ecosystem"
requires-python = ">=3.14"

dependencies = [
    "pyyaml"
]

[project.scripts]
athena-deepseek = "deepseek_importer.cli:main"
