[project]
name = "athena-git-repository-importer"
version = "0.1.0"
description = "Git repository importer for Athena Tool Ecosystem"
requires-python = ">=3.14"

dependencies = [
    "athena-tool-sdk"
]

[project.scripts]
athena-git-import = "git_repository_importer.cli:main"
